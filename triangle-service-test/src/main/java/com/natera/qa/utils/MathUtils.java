package com.natera.qa.utils;

import java.math.MathContext;
import java.math.RoundingMode;

public class MathUtils {

    public static MathContext DEFAULT_MATH_CONTEXT = new MathContext(4, RoundingMode.HALF_EVEN);
}
